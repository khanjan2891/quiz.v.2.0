<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bvm.ui_example_4">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".Teacher_createQuiz"></activity>
        <activity android:name=".Teacher_assignment" />
        <activity android:name=".Teacher_Subjects" />
        <activity android:name=".Teacher_Second" />
        <activity android:name=".quiz_main" />
        <activity android:name=".Assignment" />
        <activity android:name=".Profile" />
        <activity android:name=".Quiz" />
        <activity android:name=".Third" />
        <activity android:name=".Second" />
        <activity android:name=".MainActivity" />
        <activity android:name=".front_intro">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>